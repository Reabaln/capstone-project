apiVersion: v1
kind: Service
metadata:
  name: {{ include "sock-shop-chart.fullname" . }}
  labels:
    {{- include "sock-shop-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sock-shop-chart.selectorLabels" . | nindent 4 }}
